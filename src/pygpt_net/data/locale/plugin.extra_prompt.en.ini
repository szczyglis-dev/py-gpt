[LOCALE]
plugin.name = System Prompt Extra (append)
plugin.description = Appends extra system prompt from list to every system prompt

prompts.label = Extra prompts
prompts.description = Prompt that will be appended to every system prompt. All active extra prompts will be appended to the system prompt in the order they are listed here.
